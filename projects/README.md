# CAD projects

Create each project in `projects/<project-slug>/` by copying `projects/_template/`. Keep source requirements, decisions, model files, exports, and validation together so an agent can resume work without relying on chat history.

Do not place generated documentation corpus files here. Do not put project-specific work into `_template`.
